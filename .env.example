# --- API server settings ---
# HOST_BIND_IP: The IP address the API server will bind to.
# Use 127.0.0.1 for local access only, 0.0.0.0 for external access.
HOST_BIND_IP=127.0.0.1
HOST_PORT=8000

# --- Database settings ---
# These are used by Docker Compose to initialize the PostgreSQL container.
# The `DATABASE_URL` is constructed dynamically by the application based on these
# values, so it does not need to be set directly here.
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB_NAME=fastapi_boilerplate
